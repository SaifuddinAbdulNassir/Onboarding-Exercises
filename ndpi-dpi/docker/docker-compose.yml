services:
  dev:
    build: app
    working_dir: /workspace/app
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ..:/workspace/app
    env_file:
      - .env
    command: sleep infinity
    